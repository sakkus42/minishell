#include "../include/minishell.h"

void	input_parser(void)
{
	// t_data.inp_parser = ft_split(t_data.input, ' ');
	// int i = 0;
	// while (t_data.inp_parser[i])
	// 	printf("%s\n", t_data.inp_parser[i++]);
}